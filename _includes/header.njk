<div id="header">
  <h1>
    <a id="sitename" href="{{config.baseurl}}">{{config.title}}</a>
  </h1>

  <!-- Add something about you in p tag-->
  <p>{{ config.header }}</p>
  <hr/>

  {%- for nav in collections.nav%}
    <span class="credits pull-left">
      <a href="{{ nav.url | url }}">{{ nav.data.navtitle }}</a> |
    </span>
  {%- endfor -%}

  {# <span class="credits pull-right social">
    {% for sl in site.data.personal.social %}
      {% unless sl[1] == '' %}
        <a href="{{sl[1]}}" target="_blank">
          <i class="fa fa-{{sl[0]}}"></i>
        </a>
      {% endunless %}
    {% endfor %}
  </span> #}

</div>